import { ExpireDateValidator } from './expire-date-validator';

describe('ExpireDateValidator', () => {
  it('should create an instance', () => {
    expect(new ExpireDateValidator()).toBeTruthy();
  });
});
